# Paperclip Owner Access Report

Status: sandbox access proven, owner login not fully proven
Date: 2026-05-06
Scope: Phases 051-060

## Summary

Paperclip was tested in sandbox-only mode for local loopback and Tailnet access. No public Cloudflare route was created. No persistent service was created. The sandbox server was stopped after testing.

## Phase Results

- Phase 051: Local Paperclip URL confirmed when sandbox server is running: `http://localhost:3100`.
- Phase 052: Default localhost behavior confirmed. With no server running, `localhost:3100` refuses connection. When the sandbox server is intentionally started in loopback mode, the UI and API respond on `localhost:3100`.
- Phase 053: `curl http://localhost:3100/api/health` returned HTTP 200 while the loopback sandbox was running.
- Phase 054: Tailnet-only access is the recommended owner-access direction for this stage. It keeps Paperclip private and avoids public exposure.
- Phase 055: Tailnet onboarding was tested in sandbox using the locally cloned CLI instead of fetching through `npx`. This avoided adding a new package source during the audit.
- Phase 056: Tailnet URL confirmed during sandbox run: `http://100.116.35.95:3100`.
- Phase 057: No public Cloudflare route was created.
- Phase 058: Auth mode confirmed in Tailnet sandbox: authenticated/private with auth ready.
- Phase 059: Owner login is not fully proven. The UI is reachable on Tailnet and Paperclip generated a bootstrap invite, but the invite is treated as sensitive and was not recorded in this report. Paperclip also warned that auth base URL was not explicitly configured, so callbacks may need an explicit Tailnet base URL before owner login can be considered proven.
- Phase 060: This owner access report was created.

## Health Results

Loopback sandbox:
- UI: HTTP 200.
- Health: HTTP 200.
- Health status: `ok`.
- Mode: local trusted/private.
- Bind: loopback.

Tailnet sandbox:
- UI: HTTP 200.
- Health: HTTP 200.
- Mode: authenticated/private.
- Bind: Tailnet.
- Auth: ready.
- Bootstrap status: pending with active invite.

## Safe Owner URL

Use this only when the sandbox or future approved Paperclip service is intentionally running:

`http://100.116.35.95:3100`

This is a Tailnet URL, not a public Cloudflare URL.

## Login Status

- Login UI reachable: yes.
- Auth system ready: yes.
- Bootstrap invite generated by Paperclip: yes, treated as sensitive and not included here.
- Actual owner login completed: no.
- Login blocker: explicit auth base URL should be configured and verified for Tailnet before declaring owner login proven.

## Safety Confirmation

- No public Paperclip exposure was created.
- No public Cloudflare route was created.
- No persistent Paperclip service was created.
- No production secrets were used.
- No Mission Control database was used.
- No OpenCloud production data was used.
- No external writes were performed.
- No Zapier, HeyGen, SMB, or farmer actions were performed.
- No `.env` file in Mission Control was modified.
- No secrets were committed.
- Sandbox listener was stopped after testing.

## Remaining Blockers

- Owner login is not fully proven until the owner completes login through the Tailnet URL.
- Tailnet auth callbacks may require explicit base URL configuration before persistent access.
- Paperclip still has the previously recorded Tailnet environment test blocker in its own suite.
- Paperclip must not be persisted as a service until tests and auth are closed or formally waived.

## Recommended Next Step

Configure Paperclip sandbox auth with an explicit Tailnet base URL, restart the sandbox, regenerate a controlled owner bootstrap invite, and have the owner complete login over Tailnet. Only then consider a local-only or Tailnet-only persistent service plan.
