import { DesignerGatewayMockFrame } from '@/components/gateway/DesignerGatewayMockFrame'

export const dynamic = 'force-dynamic'

export default async function GatewayBridgeSessionPage() {
  return <DesignerGatewayMockFrame title='Gateway Bridge Session Flow' page='Bridge Session Flow.html' />
}
