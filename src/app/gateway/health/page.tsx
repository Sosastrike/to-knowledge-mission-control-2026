import { DesignerGatewayMockFrame } from '@/components/gateway/DesignerGatewayMockFrame'

export const dynamic = 'force-dynamic'

export default async function GatewayHealthPage() {
  return <DesignerGatewayMockFrame title='Gateway Health' page='Gateway Health.html' />
}
