import { DesignerGatewayMockFrame } from '@/components/gateway/DesignerGatewayMockFrame'

export const dynamic = 'force-dynamic'

export default async function GatewayNodeDetailPage() {
  return <DesignerGatewayMockFrame title='Gateway Node Detail' page='Gateway Node Detail.html' />
}
