import { DesignerGatewayMockFrame } from '@/components/gateway/DesignerGatewayMockFrame'

export const dynamic = 'force-dynamic'

export default async function GatewayOverviewPage() {
  return <DesignerGatewayMockFrame title='Gateway Overview' page='Gateway Overview.html' />
}
