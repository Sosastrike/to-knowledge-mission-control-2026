import { DesignerGatewayMockFrame } from '@/components/gateway/DesignerGatewayMockFrame'

export const dynamic = 'force-dynamic'

export default async function GatewayPoliciesPage() {
  return <DesignerGatewayMockFrame title='Gateway Policies / Bridge' page='Gateway Policies.html' />
}
