import { DesignerGatewayMockFrame } from '@/components/gateway/DesignerGatewayMockFrame'

export const dynamic = 'force-dynamic'

export default async function GatewayRegistryPage() {
  return <DesignerGatewayMockFrame title='Gateway Registry' page='Gateway Registry.html' />
}
