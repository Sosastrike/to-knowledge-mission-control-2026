import { DesignerGatewayMockFrame } from '@/components/gateway/DesignerGatewayMockFrame'

export const dynamic = 'force-dynamic'

export default async function GatewayRoutesPage() {
  return <DesignerGatewayMockFrame title='Gateway Routes' page='Gateway Routes.html' />
}
