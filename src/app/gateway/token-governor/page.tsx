import { DesignerGatewayMockFrame } from '@/components/gateway/DesignerGatewayMockFrame'

export const dynamic = 'force-dynamic'

export default async function GatewayTokenGovernorPage() {
  return <DesignerGatewayMockFrame title='Gateway Token Governor' page='Token Governor.html' />
}
